import Faqs from "@/sections/faqs";

const Home = () => {
  return (
    <main className=" min-h-screen w-full flex items-center justify-center">
      <Faqs />
    </main>
  );
}

export default Home;